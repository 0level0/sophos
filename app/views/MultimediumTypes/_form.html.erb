<%= simple_form_for(@multimedium_type) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :media_type %>
    <%= f.input :media_format %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

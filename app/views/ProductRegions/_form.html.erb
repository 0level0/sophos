<%= simple_form_for(@product_region,html:{class:'form-horizontal'}) do |f| %>
  <% if @product_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_region.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @product_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_region %><br>
    <%= f.text_field :product_region %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
